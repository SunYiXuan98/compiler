int fib(int n)
{
	if(n == 0) return(1);
	else if(n == 1)return(1);
	else return(fib(n-1) + fib(n-2));
}

void main()
{
	int n;
	while(n<=10){
		printf(fib(n));
		printf(" ");
		n=n+1;
	}
	
}
git commit -m "实现了const定义常量，可以在全局定义段、局部定义段、参数列表